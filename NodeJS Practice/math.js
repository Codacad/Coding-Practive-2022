const add = (a,b) => a + b;
const sub = (a,b) => a - b;
const mult = (a,b) => a * b;
const div = (a,b) => a / b;
const hw = (a,b) => "Hello World!";

// Exporting the whole module

module.exports = {add, sub, mult, div, hw}
